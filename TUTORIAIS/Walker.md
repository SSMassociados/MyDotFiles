------

yay -S walker-bin elephant-bin

yay -S elephant-providerlist-bin elepnant-desktopapplications-bin

pacman -Ql walker-bin | grep toml 

mkdir -p ~/.config/walker/

cp -r /etc/xdg/walker/* ~/.config/walker/